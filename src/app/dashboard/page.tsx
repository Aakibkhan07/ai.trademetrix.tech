import { DashboardPage } from './dashboard-client';

export const metadata = {
  title: 'Dashboard',
  description: 'Your algo trading control center',
};

export default function Dashboard() {
  return <DashboardPage />;
}
